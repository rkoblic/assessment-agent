[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "uvicorn src.server:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/domain/standards"
restartPolicyType = "ON_FAILURE"
